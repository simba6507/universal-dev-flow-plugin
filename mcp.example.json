{
  "_comment": "Template only. Claude Code loads .mcp.json, NOT this file. To enable a server, copy its entry into .mcp.json's mcpServers, then UNCOMMENT the matching mcp__ line in the relevant agent's tools list. Add only what you actually use to keep context lean.",
  "mcpServers": {
    "semgrep":    { "command": "npx", "args": ["-y", "semgrep-mcp"] },
    "osv":        { "command": "npx", "args": ["-y", "osv-mcp"] },
    "github":     { "command": "npx", "args": ["-y", "@modelcontextprotocol/server-github"] },
    "playwright": { "command": "npx", "args": ["-y", "@playwright/mcp"] }
  }
}
